<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"  />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/app_mvd_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_mvd_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AiCameraSplashScreen"
        android:name=".AiApplication"
        tools:targetApi="31">

        <activity
            android:name=".ui.screens.slider.SliderActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.App.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.screens.home.HomeActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".ui.screens.mapview.OsmMapActivity"
            android:screenOrientation="portrait"/>
        <service
            android:name=".service.AiCameraLocationUpdateService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="location" />
        <receiver android:name=".boardcast.StopServiceReceiver"/>

        <receiver android:name=".boardcast.NotificationDismissReceiver"
            android:exported="true">

        </receiver>


     <!--   <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove">
        </provider>-->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="remove">
        </provider>


    <!--    <receiver android:name=".geofencing.GeofenceBroadcastReceiverTwo"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ramzmania.aicammvd.ACTION_RECEIVE_GEOFENCE"/>

            </intent-filter>
        </receiver>-->

        <receiver
            android:name=".boardcast.GeoFencingBroadcastReceiver"
            android:enabled="true"
            android:exported="true">
            <!--            <intent-filter>-->
            <!--                <action android:name="com.google.android.gms.location.geofencing.ACTION_GEOFENCE_TRANSITION" />-->
            <!--            </intent-filter>-->
        </receiver>

    </application>

</manifest>